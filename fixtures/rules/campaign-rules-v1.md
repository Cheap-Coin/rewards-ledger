# Fixture campaign rules

This synthetic devnet campaign distributes a fixed native SOL test budget to the
two addresses in its checked-in allocation list. It conveys no entitlement and
must never be used for production.
